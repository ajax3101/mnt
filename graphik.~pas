unit graphik;

interface

uses
  Windows, Messages, SysUtils, Classes, Graphics, Controls, Forms, Dialogs,
  TeEngine, Series, ExtCtrls, TeeProcs, Chart, StdCtrls;

type
  Tgraphik_ = class(TForm)
    Chart1: TChart;
    Series1: TLineSeries;
    Series2: TLineSeries;
    procedure FormClose(Sender: TObject; var Action: TCloseAction);
  private
    { Private declarations }
  public
    { Public declarations }
  end;

var
  graphik_: Tgraphik_;

implementation

{$R *.DFM}

procedure Tgraphik_.FormClose(Sender: TObject; var Action: TCloseAction);
begin
     action:=cafree;
end;

end.
